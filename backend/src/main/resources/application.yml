spring:
  datasource:
    url: jdbc:h2:mem:fitting
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    show-sql: true

  h2:
    console:
      enabled: true